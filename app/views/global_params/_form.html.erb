<%= form_for(@global_param) do |f| %>
  <% if @global_param.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@global_param.errors.count, "error") %> prohibited this global_param from being saved:</h2>

      <ul>
      <% @global_param.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
